<h2> New Forecast Location </h2> 


<%= form_for(@location) do |f| %>

    <%= f.label :title %>
    <%= f.text_field :title %>
    <br> 
    <br> 

    <%= f.label :content %>
    <%= f.text_field :content %>
    <br> 
    <br> 

    <%= f.submit "Submit"  %> 

<% end %>